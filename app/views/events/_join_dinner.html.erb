<%= form_for(current_user.events.build(category: "dinner", date: day[:date])) do |f| %>
  <div><%= f.hidden_field :category %></div>
  <div><%= f.hidden_field :date %></div>
  <%= f.submit "Join", class: "btn btn-primary join-dinner" %>
<% end %>